import { META_ATTRIBUTION_CHANGE_ISO } from "@/lib/periodCompare";

/** Párrafo corto reutilizable en tooltips / ayuda (convención global spec 2026-04-22). */
export const ATTRIBUTION_POST_2026_TOOLTIP =
  `Las ventanas de atribución disponibles en Ads Insights cambiaron tras el ${META_ATTRIBUTION_CHANGE_ISO} (Meta dejó de exponer algunas ventanas view largas). Los CPA/ROAS mostrados siguen la respuesta actual de la API para el periodo seleccionado.`;
